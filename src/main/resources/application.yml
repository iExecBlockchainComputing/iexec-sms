server:
  port: ${IEXEC_SMS_PORT:15000}

# Not sure it's a good idea but here is a link for an embedded mongodb
# https://www.baeldung.com/spring-boot-embedded-mongodb
spring:
  data:
    mongodb:
      database: iexec-sms
      host: ${MONGO_HOST:localhost}
      port: ${MONGO_PORT:37017}

